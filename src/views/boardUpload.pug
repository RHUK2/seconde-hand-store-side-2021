extends layouts/main

block content 
        form.board-upload#jsBoardUpload(action=`/board${routes.boardUpload}`, method="post", enctype="multipart/form-data")
            div.board-upload__preview#jsBoardUploadPreview
                img(src=null)
                img(src=null)
                img(src=null)
                img(src=null)
            div.board-upload__upload
                label(for="photo-upload") 사진 선택
                input(type="file", id="photo-upload", name="photo", accept="image/*", multiple, required=true)
            input.board-upload__title(type="text", name="title", placeholder="제목" required=true)
            input.board-upload__cost(type="number", name="cost", placeholder="가격" required=true)
            div.board-upload__select
                label(for="area-select") 지역 선택:
                select(name="areas", id="area-select" required=true)
                    option(value="") -----------
                    each area in areas
                        option(value=area)=area
            div.board-upload__search
                p 동네 이름을 검색 후(ex. 구로구, 영등포구) 만남을 가질 장소를 클릭해주세요.
                input(hidden="hidden")
                input#jsSearchArea(type="text", placeholder="동네 검색")
            div.board-upload__map-data
                h4 직거래 장소:
                input#jsUploadMapData(type="text", placeholder="만나실 장소를 클릭해주세요.(필수)", name="address" required=true, autocomplete="off")
            div.board-upload__map#jsUploadMap
            textarea.board-upload__description(name="description", placeholder="제품 정보를 작성해주세요.", required=true)
            input.board-upload__submit(type="submit", value="업로드")
